#include "../../header/graphics.h"

void	put_sword(t_graphics **graph)
{
	int	x;
	int	y;

	x = (WIDTH / 2) - ((*graph)->width_sword / 2);
	y = HEIGHT - (*graph)->height_sword;
	while (x < (*graph)->width_sword)
	{
		while (y < HEIGHT)
		{
			if ()
		}
	}
}
